import { IStoreForm } from '../model/store-form.model';
import { IStoreRequest } from '../model/store-request.model';

export const createStoreMapper = (ownerForm: IStoreForm): IStoreRequest => {
  return {} as IStoreRequest;
};
