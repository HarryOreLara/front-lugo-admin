import { NroCuentaBancoPipe } from './nro-cuenta-banco.pipe';

describe('NroCuentaBancoPipe', () => {
  it('create an instance', () => {
    const pipe = new NroCuentaBancoPipe();
    expect(pipe).toBeTruthy();
  });
});
